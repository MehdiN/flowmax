[project]
dependencies = ["jax","equinox","diffrax","jaxtyping"]
name = "flowmax"
requires-python = ">=3.12"
version="0.1.0"


[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[tool.hatch.build.targets.wheel]
packages = ["flowmax"]
